import { createTheme } from "@material-ui/core/styles";

const theme = createTheme({
  typography: {
    fontFamily: "Poppins",
  },
  palette: {
    primary: {
      main: "#b9f6ca",
      light: "#ecfffd",
      dark: "#88c399",
    },
  },
});

export default theme;
